```yaml
搜索顺序:
  a. 在前端路由配置中搜索路径关键词:
     Grep(pattern="{route_keyword}", path="{{frontend.projectPath}}/src")
     目标文件: router/index.js 或 src/router/routes/ 下的路由模块
     匹配: path: '/xxx' 路由配置

  b. 从路由配置找到页面组件的 import 路径:
     如: component: () => import('@/views/xxx/index.vue')
     或: component: resolve => require(['@/views/xxx/index'], resolve)
     → 页面组件: src/views/xxx/index.vue

  c. 读取页面组件（.vue SFC），记录:
     - 文件路径
     - <script> 中的所有 import 语句
     - 引入的 mixin 文件
     - 引入的 service / API 文件
     - 引入的子组件（components 选项注册）
     - 使用的 UI 组件（{{frontend.uiLibrary}} 组件名）
     - 页面包含的功能:
       - 表格组件（如 ElTable / ATable）
       - 表单组件（如 ElForm / AForm）
       - 按钮（新增、编辑、删除、导出等）
       - 弹窗/抽屉（ElDialog / AModal / ElDrawer）
       - 搜索/查询区域
       - 分页组件

  d. 分析 Options API:
     - data() 声明的数据属性
     - computed 计算属性
     - watch 监听器
     - methods 方法定义
     - mounted/created 等生命周期钩子
     - mixins 混入
     - components 局部注册的子组件
```
