```yaml
表名提取方式（ASP.NET Core + Entity Framework Core）:
  a. 从 Entity 的 [Table] 特性提取:
     搜索: Grep(pattern="\\[Table\\(", path="{{backend.projectPath}}")
     示例:
       [Table("bos_orders")]
       public class Order { ... }
     → 表名: bos_orders

  b. 从 DbContext 的 OnModelCreating 配置提取:
     搜索: Grep(pattern="ToTable\\(", path="{{backend.projectPath}}")
     示例: modelBuilder.Entity<Order>().ToTable("bos_orders")
     → 表名: bos_orders

  c. 从 DbSet<T> 属性名推断默认表名:
     EF Core 默认使用 DbSet 属性名作为表名
     示例: public DbSet<Order> Orders { get; set; } → 表名: Orders

  d. 从 [Column] 特性提取字段映射:
     示例: [Column("order_no")] → 数据库字段: order_no

  e. 从 Migration 文件确认表名（如有）:
     搜索: Glob(pattern="{{backend.projectPath}}/**/Migrations/*.cs")
     查看 migrationBuilder.CreateTable() 中的表名

  f. 汇总所有关联表名:
     - 主表: 从 [Table] 特性或 ToTable() 配置
     - 关联表: 从导航属性和 HasOne/HasMany/ManyToMany 关系
     - 中间表: 从 UsingEntity() 配置
```
