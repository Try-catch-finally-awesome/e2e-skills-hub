```yaml
表名提取方式（PHP ThinkPHP + think-orm）:
  a. 从 Model 的 $table 属性提取:
     搜索: Grep(pattern="\\$table\\s*=", path="{{backend.projectPath}}/app/model")
     示例:
       protected $table = 'bos_orders';
     → 表名: bos_orders

  b. 从 Model 的 $name 属性提取:
     搜索: Grep(pattern="\\$name\\s*=", path="{{backend.projectPath}}/app/model")
     示例:
       protected $name = 'order';
     → 表名: order（ThinkPHP 会自动加上配置的表前缀）

  c. 从 Model 类名推断默认表名:
     如 Model 没有定义 $table 或 $name 属性:
     ThinkPHP 默认规则: 将类名转为蛇形命名作为表名
     示例: OrderDetail → order_detail
     全局配置: config/database.php 中的 prefix 会添加表前缀

  d. 从 Db 类直接查询中提取:
     搜索: Grep(pattern="Db::table|Db::name|Db::query", path="{{backend.projectPath}}/app")
     示例:
     - Db::table('bos_orders') → 表名: bos_orders（含前缀的完整表名）
     - Db::name('order') → 表名: order（自动加前缀）

  e. 汇总所有关联表名:
     - 主表: 从 Model 的 $table/$name 属性提取
     - 关联表: 从 hasOne/hasMany/belongsTo/belongsToMany 关联方法中提取
     - 子表: 从 Service 层的多表操作中提取
```
