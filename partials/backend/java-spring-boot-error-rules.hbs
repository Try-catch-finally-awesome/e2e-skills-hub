```yaml
判断条件（满足任一即归因为后端 - Java Spring Boot）:
  - 后端返回 HTTP 5xx（通过透传的错误信息判断）
  - SQL 语法错误、字段不存在、约束违反（MyBatis Plus 异常）
  - 业务逻辑错误（状态流转不正确、计算错误、规则校验错误）
  - 数据库查询结果与预期不符（写入了错误数据）
  - MQ 消息处理失败
  - MyBatis Plus Mapper 配置错误

常见异常类型:
  - NullPointerException: 空指针，通常是查询结果为空未处理
  - IllegalArgumentException: 参数校验失败
  - DataIntegrityViolationException: 数据库约束违反（唯一索引、外键等）
  - BadSqlGrammarException: SQL 语法错误（字段名拼写、表名错误）
  - BindException / MethodArgumentNotValidException: @Valid 校验失败
  - DuplicateKeyException: 唯一键冲突
  - MybatisPlusException: MyBatis Plus 配置或映射错误
  - HttpMessageNotReadableException: 请求体 JSON 解析失败
  - TypeMismatchException: 参数类型不匹配
  - NoHandlerFoundException: 接口路径不存在
  - TransactionSystemException: 事务提交失败

修复范围: {{backend.projectPath}}/src/ 下的文件
编译验证: cd {{backend.projectPath}} && {{backend.buildCommand}}

服务重启提示:
  - Java 代码修改后需要重启 Spring Boot 服务
  - 命令: cd {{backend.projectPath}} && mvn spring-boot:run
  - 或在 IDE 中重新启动
```
