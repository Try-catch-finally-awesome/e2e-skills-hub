---
name: e2e-report-generator
description: 汇总 E2E 测试执行结果、错误修复记录、数据库验证数据，生成完整的集成测试报告并输出到 {{config.report.outputPath}} 目录。
allowed-tools: Read, Write, Glob, Grep
---

# E2E Report Generator - 测试报告生成专家

## 职责

汇总所有测试阶段的数据，生成完整的 E2E 集成测试报告。

## 输入

汇总以下阶段的输出：
- `e2e-code-tracer`: 代码溯源链
- `e2e-testcase-generator`: 测试用例清单
- `e2e-playwright-runner`: 执行结果（通过/失败、截图）
- `e2e-error-fixer`: 修复记录（归因、修复内容、重测结果）

## 输出路径

```
{{config.report.outputPath}}/{页面路径}_E2E测试报告_{YYYYMMDD}.md
```

## 报告模板

```markdown
# {页面名称} E2E 集成测试报告

## 1. 测试概要

| 项目 | 内容 |
|------|------|
| 测试页面 | {page_url} |
| 测试日期 | {date} |
| 测试方式 | Playwright MCP 前端驱动 E2E |
| 涉及项目 | {{backend.projectName}}{{#if middlePlatform}} / {{middlePlatform.projectName}}{{/if}} / {{frontend.projectName}} |
| 用例总数 | {total} |
| 首轮通过 | {first_pass} |
| 修复后通过 | {fixed_pass} |
| 需人工介入 | {manual} |
| 总修复次数 | {fix_count} |

## 2. 代码溯源链

| 层级 | 文件 |
|------|------|
| 前端页面 | {page_component_path} |
| 前端 Service | {service_path} |
{{#if middlePlatform}}
| 中台 Controller | {mid_controller_path} |
| 中台 BLL | {mid_bll_path} |
{{/if}}
| 后端 Controller | {backend_controller_path} |
| 后端 Service | {backend_service_path} |
| 后端 Repository | {backend_repository_path} |
| 数据库表 | {table_names} |

## 3. 测试用例执行结果

| 编号 | 分类 | 用例名称 | 首轮结果 | 最终结果 | 修复次数 | 备注 |
|------|------|---------|---------|---------|---------|------|
| TC-001 | 页面加载 | 页面正常加载 | 通过 | 通过 | 0 | - |
| TC-030 | 新增 | 正常新增提交 | 失败 | 通过 | 1 | 后端SQL修复 |

## 4. 错误归因与修复记录

### 按项目统计

| 项目 | 错误数 | 修复数 | 未修复 |
|------|--------|--------|--------|
| 前端 ({{frontend.projectName}}) | {fe_errors} | {fe_fixed} | {fe_open} |
{{#if middlePlatform}}
| 中台 ({{middlePlatform.projectName}}) | {mid_errors} | {mid_fixed} | {mid_open} |
{{/if}}
| 后端 ({{backend.projectName}}) | {be_errors} | {be_fixed} | {be_open} |

### 修复明细

| 修复编号 | 关联用例 | 归因项目 | 问题描述 | 修复文件 | 修复内容 | 验证结果 |
|---------|---------|---------|---------|---------|---------|---------|
| FIX-001 | TC-030 | 后端 | {描述} | {文件:行号} | {内容} | 通过 |

{{#if mcp.database}}
## 5. 数据库验证结果

| 验证项 | SQL | 预期 | 实际 | 状态 |
|--------|-----|------|------|------|
| 新增数据写入 | SELECT ... | 记录存在 | {实际} | 通过 |
| 字段值正确 | SELECT ... | {预期值} | {实际值} | 通过 |
{{/if}}

## {{#if mcp.database}}6{{else}}5{{/if}}. 综合评分

| 维度 | 权重 | 得分 | 加权得分 |
|------|------|------|---------|
| 页面加载与渲染 | {{config.quality.scoreWeights.pageLoad}}% | {score} | {weighted} |
| CRUD 功能正确性 | {{config.quality.scoreWeights.crudCorrectness}}% | {score} | {weighted} |
| 业务流程正确性 | {{config.quality.scoreWeights.businessFlow}}% | {score} | {weighted} |
| 异常处理与提示 | {{config.quality.scoreWeights.exceptionHandling}}% | {score} | {weighted} |
| 数据库数据一致性 | {{config.quality.scoreWeights.dataConsistency}}% | {score} | {weighted} |
| **综合评分** | **100%** | | **{total_score}** |

### 评分标准

| 分数 | 条件 |
|------|------|
| 100 | 首轮全部通过 |
| 95 | 修复后全部通过，修复次数 <= 3 |
| 90 | 修复后全部通过，修复次数 <= 10 |
| 85 | 修复后全部通过，修复次数 > 10 |
| <85 | 存在需人工介入的用例 |

### 维度评分规则

| 维度 | 满分条件 | 扣分规则 |
|------|---------|---------|
| 页面加载与渲染 | 所有页面加载类用例通过 | 每失败 1 个用例扣 30 分 |
| CRUD 功能正确性 | 所有 CRUD 用例通过 | 每失败 1 个用例扣 20 分 |
| 业务流程正确性 | 所有业务流程用例通过 | 每失败 1 个用例扣 25 分 |
| 异常处理与提示 | 所有异常场景用例通过 | 每失败 1 个用例扣 25 分 |
| 数据库数据一致性 | 所有数据库验证通过 | 每失败 1 个验证扣 30 分 |

### 修复惩罚系数

- 0 次修复: 不扣减
- 1-3 次修复: 扣 5 分
- 4-10 次修复: 扣 10 分
- 11-20 次修复: 扣 15 分
- 存在人工介入: 额外扣 15 分

## {{#if mcp.database}}7{{else}}6{{/if}}. 结论

- 评分: {score}/100
- 状态: {通过/不通过}（通过线: {{config.quality.passScore}} 分）
- 所有用例是否全部通过: {是/否}
```
